---
name: PC01 (proposal-changes-01) progress
description: Status of user-story plans under docs/propose-changes-01/quality-gate-evaluators.feature; each US lands as a new audit-rule kind or evaluator extension.
type: project
---

PC01 = "proposal-changes-01" — declarative quality-gate evaluators driven by the framework profile YAML. Each user story adds a new `AuditRuleKind` (or extends one) plus its evaluator function in `internal/domain/service/auditRuleEvaluator.go`, and whitelists it in `internal/infrastructure/fsprofile/mapper.go`.

**Why:** the engine is intentionally language-neutral — `Entity`/`Spring`/`JUnit` literals must NEVER appear in `internal/domain` or `internal/application`; they live only in profile YAML and testdata fixtures. PC01RNF-001 enforces this; the discovery skill greps for these tokens before declaring a Tier 1 group done.

**How to apply:** when the user asks for a new PC01US-NNN plan, follow the established shape — one new `AuditKindXxx` constant, one `evalXxx` function, mapper whitelist, plus testdata fixtures named `pc01usNNN<FeatureCamel>/projectXxx`. Tiers usually collapse to {1, 2, 6} (no application/presentation/wiring change). Reuse helpers: `splitNonEmpty`, `nodeTypeAllowed`, `pathExempt`, `matchPathGlob`, `makeViolation`, `substituteSuggestion`.

User-story plan status (as of 2026-04-30):
- PC01US-002 (usecase-impl-stereotype) — plan landed.
- PC01US-003 (jpa-entity-contract) — plan landed.
- PC01US-004 (forbid-autowired-field-injection) — plan landed; introduced `target=field`, `exempt_paths`, `matchPathGlob`.
- PC01US-005 (test-method-naming) — plan landed; introduced `JavaMethod.{Name,Annotations,Line}`, `triggered_by`, `name_pattern`.
- PC01US-006 (unit-test-class-contract) — plan landed; introduced `JavaDeclaration.AnnotationArgs`, `expected_values` param on `required_annotations`.
- PC01US-007 (domain-no-entity-collection) — plan landed 2026-04-29; introduces `AuditKindForbiddenFieldTypePattern`, helpers `matchTypePattern` and `resolveFQN`.
- PC01US-008 (usecase-supertype, parameterized `UseCase<I, O>`) — plan landed; introduces `AuditKindRequiredParameterizedSupertype`, `JavaDeclaration.ParameterizedSupertypes`, helpers `parseSupertypePattern`/`splitGenericArgs`. Implementation already in main as of commit 8746a60.
- PC01US-009 (integration-test-base required annotations: `@SpringBootTest` + `@Testcontainers` + `@ActiveProfiles("test")`) — plan landed 2026-04-30 at `.claude/plans/pc01-us-009-it-base-required-annotations/plan.md` as a **tests-only ratification**. The existing `required_annotations` kind with `expected_values` param (PC01US-006) already covers AC1/AC2/AC3 — Tiers 1-5 are N/A; only Tier 6 ships (1 integration test + 3 fixture trees). Q3 documents that the parser captures string-literal args verbatim WITH quotes, so `expected_value="test", actual="prod"` is the literal output (not the AC's `expected_value=test, actual=prod` without quotes); the integration test asserts on substrings.
- PC01US-010 (transactional decorator: @Primary + @Qualifier(*)) — plan landed 2026-04-30 at `.claude/plans/pc01-us-010-tx-decorator-contract/plan.md` and implemented same day. Existing `required_annotations` kind covers AC1/AC2 (all-of semantics); AC3 ("non-empty Qualifier value") needed a minimal Tier 1 extension — additive optional sibling param `non_empty_value_annotations` on `evalRequiredAnnotations` plus private helper `isEmptyAnnotationArg` (treats `""`, `"\"\""`, `"''"` as empty). Determinism order: missing → expected_values → non-empty. Tiers active: 1, 6 (Tiers 2-5 N/A — no model/mapper/UC/CLI changes). Engine-neutrality (PC01RNF-001) preserved — `Primary`/`Qualifier`/`TxDecorator` literals confined to fixtures, integration tests, and the unit-test rule helper.
- PC01US-012 (preserve backward compatibility with legacy `has_annotation`) — plan landed 2026-04-30 at `.claude/plans/pc01-us-012-legacy-has-annotation/plan.md` and implemented same day. Tiers active: 2, 6 (Tiers 1/3/4/5 N/A — no domain/UC/CLI/wire change). New pure infra helper `translateLegacyHasAnnotation` in `internal/infrastructure/fsprofile/legacyHasAnnotation.go` folds legacy `has_annotation: X` into modern `kind: required_annotations` + `params.annotations = X` shape. Both DTOs (`auditRuleDTO`, `bundleAuditRuleDTO`) gained `HasAnnotation string yaml:"has_annotation"` to keep `KnownFields(true)` decoders accepting the legacy shortcut. Translation runs FIRST in both loaders (`auditLoader.LoadAuditRules` and `bundleMapper.toBundleDomain`) — BEFORE the kind whitelist and BEFORE PC01US-011's `validateAuditRuleParams`. Tie-breaking: explicit `kind:` wins over `has_annotation:`; explicit `params.annotations` wins over legacy fill. Engine-neutrality (PC01RNF-001) preserved — only neutral schema words (`has_annotation`, `required_annotations`) introduced. AC1 integration test required restructuring the fixture from a bundle directory to a single-file profile + `pom.xml` + populated `project-state.yaml` modules (matching PC01US-010 pattern) so module discovery picks up PlaceOrder.java; the audit cmd does NOT exit non-zero on ERROR-severity violations (matching all prior audit integration tests' `require.NoError` posture). No new reserved param keys — `has_annotation` is a YAML field, not a `params` key.
- PC01US-011 (validate new YAML schema in `jitctx profile validate`) — plan landed 2026-04-30 at `.claude/plans/pc01-us-011-profile-validate-new-schema/plan.md` and implemented same day. Tiers active: 2, 6 (Tiers 1/3/4/5 N/A — no domain/UC/CLI/wire changes). New pure helper `validateAuditRuleParams` in `internal/infrastructure/fsprofile/auditRuleValidator.go` consumed by both `bundleMapper.toBundleDomain` and `auditLoader.LoadAuditRules`. Closed message catalogue M1–M4 (target enum check fires before kind-specific empty-list checks). Errors wrapped with `domerr.ErrProfileInvalid` sentinel via `fmt.Errorf("...: %w: %w", err, domerr.ErrProfileInvalid)`. No new reserved param keys. PC01RF-011's "line/column" reporting is deferred (Q-OPTIONAL — no Gherkin substring asserts on it).
- PC01US-013 (deterministic violation output) — plan landed 2026-04-30 at `.claude/plans/pc01-us-013-deterministic-violation-output/plan.md` as a **tests-only ratification**. Tiers active: 6 only (Tiers 1-5 N/A). Existing sort code at `audituc/usecase.go:217-219` (`sort.SliceStable` with `lessViolation`) and renderer purity (`WriteAuditReport`) already cover PC01RNF-003 — `TestAuditCmd_Integration_Determinism` (auditViolations fixture) and `TestWriteAuditReport_Determinism` (format layer) stayed green through PC01US-002..012. Comparator key is `(ModuleID, FilePath, Line, RuleID)` — a strict refinement of the AC's stated `(file, line, rule ID)` key (ModuleID is the natural grouping for `## Module:` sub-headings, with `<unmoduled>` forced last). New: ONE integration test `TestAuditCmd_Integration_PC01US013_DeterministicViolationOutput_TwoRunsByteIdentical` plus a dedicated PC01 fixture `testdata/pc01us013DeterministicOutput/projectFixed/` that declares TWO `required_annotations` rules (`A-pc01us013-required-service` and `B-pc01us013-required-component`) in REVERSE alphabetical YAML order so the comparator's ascending-RuleID tiebreaker is observable via `indexA < indexB` in stdout. Existing `TestAuditCmd_Integration_Determinism` stays intact.
- PC01US-014 (engine remains language-neutral, PC01RNF-001 + PC01RF-010 + R-005) — plan landed 2026-04-30 at `.claude/plans/pc01-us-014-engine-language-neutrality/plan.md` and **implemented 2026-05-01** (user ratified Q-BLOCKING-1=B, Q-BLOCKING-2=YES, Q-BLOCKING-3=case-sensitive). Final grep on `internal/{domain,application,cli}` excluding `testdata/` and the enforcement test itself: **ZERO hits**. All 28 packages green. New enforcement test `TestEngineLanguageNeutrality_NoFrameworkIdentifiers_PC01US014` at `internal/cli/command/engineLanguageNeutralityIntegration_test.go` walks the three engine roots and asserts zero matches. New `model.ProfileBundle.TestRunnerExtensionFQN` field + `test_runner_extension_fqn` YAML key + `fsprofile.DefaultProfileName` const decouple wire.go and scaffolduc from framework-specific literals. Backward-compat aliases `"jpa-adapter"` → `model.ContractPersistenceAdapter` in BOTH `mdspec/parser.go` AND `fsprofile/mapper.go::toDomain` keep existing manifests/profiles loadable. `wire.go` line that originally introduced `"spring-boot-hexagonal"` literal triggered the orthogonal EP04US-009 `TestJavaReferencesGate` — fixed by extracting `fsprofile.DefaultProfileName` and adding `bundled.go` to `qualitygate.ExemptFiles`. Per-package `testdata/forbiddenAnnotations.txt` fixtures (4 created) externalize banned-token literals from test sources. Pascal-cased `Java*` identifiers (`JavaFileSummary`, `JavaImportResolver`, `JavaDeclaration`) STAY — regex omits `Java`. PC01RNF-003 determinism suite stayed green throughout. **Original BLOCKED entry below for historical context:**
- PC01US-014 (original BLOCKED status, retained for traceability) — plan landed 2026-04-30 with three Q-BLOCKING items at the human checkpoint. Today's grep `(Lombok|Spring|Mockito|Autowired|JPA)` over the three engine roots returns 34 files / 157 matches; Approach (B) refactor-and-enforce is recommended (Approach A "enforcement-only with t.Skip" rejected for a Must-priority story; Approach C "scoped exemption" rejected without explicit user buy-in). Tiers active: 1, 3, 4, 5, 6 (Tier 2 N/A). 41 in-scope files / 8 groups; effort distribution = 1×L (T1-G1 atomic 17-file domain rename including 4 cross-context infra dependents on `ContractJPAAdapter` constant) + 1×M (T3-G1 scaffolduc rename + delete `importTestRunnerExtensionFQN` constant whose Mockito FQN trips metric) + 2×S (T4-G1 errors.go message + T5-G1 wire.go field rename) + 1×M+2×L+1×M (Tier 6 four groups: domain-test renames, audit-evaluator-test fixture externalisation via `testdata/forbiddenAnnotations.txt`, cli integration-test renames, NEW static-check test `engineLanguageNeutralityIntegration_test.go`). Frozen domain contract: `ContractJPAAdapter ContractType = "jpa-adapter"` → `ContractPersistenceAdapter ContractType = "persistence-adapter"`; `JPAFieldAnnotator` → `IDFieldAnnotator` (file rename `jpaFieldAnnotator.go` → `idFieldAnnotator.go`). Q-BLOCKING-1: A vs B vs C scope choice. Q-BLOCKING-2: hard YAML value rename `"jpa-adapter"` → `"persistence-adapter"` (breaking for existing manifests; soft-rename infeasible because keeping the literal `"jpa-adapter"` string in `model/contract.go` itself trips the case-sensitive `JPA` regex). Q-BLOCKING-3: `JavaImportResolver` Spring-FQN dependency — collapses to non-blocking under case-sensitive metric (recommended) but binds under case-insensitive Gherkin reading. Q-NON-BLOCKING-1: `Java*` identifiers stay (regex omits `Java`, prose mentions it; metric wins). Q-NON-BLOCKING-2: case-sensitive (matches metric, not Gherkin). Q-NON-BLOCKING-3: test-fixture detoxification via externalised `testdata/forbiddenAnnotations.txt` (option t-2). Static-check test lives at `internal/cli/command/engineLanguageNeutralityIntegration_test.go`, walks the three engine roots, asserts no `.go` line contains any of the five forbidden tokens; deterministic sorted multi-line failure output. **Static-check exemption convention `//jitctx:engine-neutrality-exempt-line` is parked as t-3 alternative if user prefers per-line directives over fixture externalisation.**

Reserved param keys after PC01US-011 (unchanged from PC01US-010): `path_scope`, `annotations`, `expected_values`, `node_types`, `target`, `exempt_paths`, `triggered_by`, `name_pattern`, `forbidden_type_patterns`, `expected_supertype`, `args`, `supertype_kind`, `path_required`, `path_required_any`, `name_suffix`, `name_regex`, `forbidden_type_suffix`, `forbidden_type_substring`, `import_prefix`, `implements_glob`, `annotation`, `non_empty_value_annotations`.
