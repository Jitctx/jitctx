package audit

// SemanticPlaceholder is the literal body rendered under the
// "## Semantic Analysis" heading. Centralised here so unit tests
// and the use case both reference the same source of truth (RNF-005).
const SemanticPlaceholder = "Semantic analysis not enabled. Future versions of jitctx may support deeper checks via analyzer plugins."
